flask>=3.1.0
qdrant-client>=1.18.0
pdfplumber>=0.11.0
openpyxl>=3.1.0
python-docx>=1.2.0
# requests is used instead of urllib.request in SSE streaming endpoints
# (hybrid_stream / search_stream) because it handles client disconnects much better.
requests>=2.32.3

# Opcjonalne - do OCR skanów PDF i obrazów (wymaga też instalacji systemowej: tesseract-ocr + poppler-utils)
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0   # do bezpośredniego odczytu obrazów w OCR

# Retry z Exponential Backoff dla błędów HTTP 429/50x
tenacity>=8.2.0

# Produkcyjny serwer WSGI (zastępuje Flask dev server)
waitress>=3.0.0

# MS SQL Server (moduł SQL w aplikacji)
pymssql>=2.3.0
