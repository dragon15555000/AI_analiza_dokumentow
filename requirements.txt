flask>=3.1.0
qdrant-client>=1.18.0
pdfplumber>=0.11.0
openpyxl>=3.1.0
python-docx>=1.2.0
# requests is used instead of urllib.request in SSE streaming endpoints
# (hybrid_stream / search_stream) because it handles client disconnects much better.
requests>=2.32.3

# Opcjonalne - do OCR skanów PDF (wymaga też instalacji systemowej: tesseract-ocr + poppler-utils)
pytesseract>=0.3.10
pdf2image>=1.17.0
